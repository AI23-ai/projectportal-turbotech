# Production Environment Variables Template
# These are managed via CloudFormation (infra/frontend-stack.yaml)
# and populated from AWS Secrets Manager.

# Auth0 Configuration
AUTH0_SECRET=<generate-new-secret-for-production>
AUTH0_BASE_URL=https://<your-app-runner-url>
AUTH0_ISSUER_BASE_URL=https://<your-auth0-tenant>.us.auth0.com
AUTH0_CLIENT_ID=<from-auth0-dashboard>
AUTH0_CLIENT_SECRET=<from-auth0-dashboard>

# API Configuration
NEXT_PUBLIC_API_URL=https://<your-api-gateway-url>

# Generate new AUTH0_SECRET with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
